# Help?

Fork the repo -> Fix/Add/Other -> Create PR into this repo -> Review -> Maybe accept -> Sweet!

# Install from source

``` shell
mvn clean install
```

# Compile from source

``` shell
mvn clean compile
```

# Run test

``` shell
mvn test
 ```